package katas.exercises.wallet;

public class Utils {
    /**
     * Enum to represent stock types (e.g., PETROLEUM, BITCOIN, USD).
     */
    public enum StockType {
        PETROLEUM, BITCOIN, USD, EURO
    }

    /**
     * Enum to represent currencies (e.g., EUR, USD).
     */
    public enum Currency {
        EUR, USD
    }
}
